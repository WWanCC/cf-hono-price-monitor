# v1.5.0

## 批量创建价格监控

- “新增监控”中的官方 SKU、自店 SKU 改为多选。
- 支持“按选择顺序一一配对”和“全部组合”两种配对方式。
- 常用规则可一次应用到本批次所有 SKU 配对。
- 提交前显示配对预览和预计创建数量。
- 前后端都限制单批最多 100 条 Monitor，避免误操作制造过多组合。
- 新增 `POST /api/monitors/batch`，数据库已存在的配对会跳过并返回统计结果。
- 批量写入按 30 条分块，确保每条 D1 SQL 的绑定参数不超过平台 100 个的限制。

## 可维护性

- 新增 `services/monitor-management.ts`，集中处理 Monitor 创建校验与批量写入。
- 重写 Monitor 页面、API 客户端、Worker 入口和主要路由，展开压缩式单行代码。
- 为认证、Listing 导入、规则解析、Monitor 检测等关键业务补充设计意图注释。
- 前端页面继续按业务拆分，`App.vue` 只保留登录态、导航和页面挂载。
- 保留 v1.4 的 PBKDF2 100000、`.dev.vars` 排除、CRUD、删除策略和旧 SKU 自动停用修复。

## 数据库

本版本没有修改数据库 Schema，因此没有新增 migration。已有 v1.4 数据库可以直接部署新代码。
